<!DOCTYPE html>
<html>
  <head>
    <title><%= locals.title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= locals.title %></h1>
    <p>Welcome to <%= locals.title %></p>
    <a href="/new"><button>New Message</button></a>
    <% for (let i = locals.messages.length - 1; i >= 0 ; i--) { %>
    <div class="parent">
      <div class="user"><%= locals.messages[i].user %></div>
      <div class="text"><%= locals.messages[i].text %></div>
      <div class="date"><%= locals.messages[i].added %></div>
    </div>
    <% } %>
  </body>
</html>
